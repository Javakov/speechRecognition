## 📋 Архитектура проекта

Проект состоит из 7 основных классов:

---

## 1️⃣ `TranscriptionSegment.java` - Модель данных

**Назначение:** Хранит один сегмент распознанной речи с таймкодами.

**Поля:**
- `startTime` (double) - время начала в секундах
- `endTime` (double) - время окончания в секундах  
- `text` (String) - распознанный текст

**Методы:**
- `toString()` - форматирует сегмент как `[0.00 - 5.00] hello world`
- Использует `Locale.US` чтобы всегда выводить точку вместо запятой в числах

---

## 2️⃣ `AudioExtractor.java` - Извлечение и конвертация аудио

**Назначение:** Извлекает аудиодорожку из видео/аудио и конвертирует в WAV 16kHz mono.

**Зависимости:** 
- `ws.schild.jave2` - обертка над FFmpeg
- Встроенный FFmpeg (не требует установки)

**Главный метод:** `extractAudio(Path inputFile) -> Path`

**Алгоритм работы:**
1. Создает временный WAV файл
2. Настраивает аудио атрибуты:
   - Кодек: PCM_SIGNED (несжатый)
   - Частота: 16000 Hz (требование Vosk)
   - Каналы: 1 (моно)
   - Битрейт: 128000 bps
3. Использует `Encoder` из Jave2 для конвертации
4. Возвращает путь к временному WAV файлу

**Причины 16kHz mono**
- Vosk модели обучены на таком формате
- Моно достаточно для распознавания речи
- 16kHz - стандарт для телефонии и речи

---

## 3️⃣ `SpeechRecognitionService.java` - Распознавание речи

**Назначение:** Распознает речь из WAV файла используя Vosk API и создает сегменты на основе пауз между словами.

**Зависимости:**
- `com.alphacephei.vosk` - Java обертка над Vosk
- Нативные библиотеки Vosk (автоматически подгружаются)

**Поля:**
- `model` (Model) - загруженная модель Vosk
- `SAMPLE_RATE` (int) - частота дискретизации (16000 Hz)
- `BUFFER_SIZE` (int) - размер буфера для чтения аудио (4000 байт)

**Конструктор:** `SpeechRecognitionService(Path modelPath)`
- Загружает модель из папки (например, `models/vosk-model-small-ru-0.22/`)
- Модель остается в памяти для быстрой работы

**Главный метод:** `recognizeSpeech(Path audioFile) -> List<TranscriptionSegment>`

**Алгоритм работы:**
1. Создает `Recognizer` с моделью и частотой 16000 Hz
2. Включает вывод информации о словах: `recognizer.setWords(true)`
3. Читает WAV файл порциями по 4000 байт
4. Подает каждый блок в `recognizer.acceptWaveForm(buffer)`
5. **Накапливает все слова из всех порций** в один список
6. Парсит JSON результаты через `extractWords()`:
   - Промежуточные результаты: `{"partial": "hello"}`
   - Финальные результаты: `{"result": [{"word": "hello", "start": 0.5, "end": 1.2}]}`
7. **Создает сегменты на основе пауз между словами** (пауза > 0.5 сек = граница сегмента)
8. Возвращает список `TranscriptionSegment`

**Особенности:**
- `acceptWaveForm()` возвращает `true` когда распознал фразу
- `getFinalResult()` вызывается в конце для последнего фрагмента
- Vosk выдает слова с точностью до миллисекунд
- **Важно:** Все слова накапливаются перед созданием сегментов - это необходимо для правильного определения пауз

**Метод `createSegmentsByPauses(List<WordInfo> words)`:**
- Группирует слова в сегменты на основе пауз между словами
- Пауза > 0.5 секунды означает границу нового сегмента (новое предложение)
- Если сегмент слишком длинный (> 10 сек), разбивает его
- Это позволяет правильно разделять предложения на разных языках

**Пример JSON от Vosk:**
```json
{
  "result": [
    {"conf": 0.95, "end": 1.23, "start": 0.78, "word": "hello"},
    {"conf": 0.89, "end": 2.45, "start": 1.23, "word": "world"}
  ],
  "text": "hello world"
}
```

**Метод `close()`:**
- Освобождает ресурсы модели
- Важно вызывать после использования

---

## 4️⃣ `LanguageDetector.java` - Определение языка и оценка качества

**Назначение:** Определяет язык текста, оценивает качество распознавания и определяет транслитерацию.

**Основные функции:**
- Определение языка текста на основе алфавита (кириллица/латиница)
- Оценка качества распознавания текста
- Определение транслитерации английских слов кириллицей

**Главные методы:**

**`detectLanguage(String text) -> String`**
- Определяет язык текста на основе соотношения кириллических и латинских символов
- Возвращает: `"ru"` (русский), `"en"` (английский), `"mixed"` (смешанный)
- Алгоритм:
  - Подсчитывает количество кириллических и латинских букв
  - Если > 60% кириллицы → `"ru"`
  - Если > 60% латиницы → `"en"`
  - Иначе → `"mixed"`

**`assessQuality(String text) -> double`**
- Оценивает качество распознавания текста (0.0 - 1.0)
- Факторы оценки:
  - Количество слов (больше слов = лучше, до 10 слов)
  - Количество букв (больше букв = лучше, до 50 букв)
  - Однородность алфавита (бонус за чистый алфавит, штраф за смешанный)
  - Количество цифр (штраф если слишком много)
  - Длина текста (бонус за разумную длину 10-200 символов)
  - **Транслитерация (сильный штраф ×0.3)**

**`isTransliteration(String text) -> boolean`**
- Определяет, является ли текст транслитерацией английских слов кириллицей
- Использует статистический анализ без захардкоженных словарей
- Признаки транслитерации:
  - Много очень коротких слов (1-3 буквы) - как английские артикли, предлоги
  - Низкая средняя длина слова (< 5.2 букв)
  - Мало длинных слов (7+ букв) - < 25%
  - Много коротких слов (1-4 буквы) - > 55%
- Если >= 30% длинных слов → это нормальный русский текст, не транслитерация
- Если 2+ критерия выполнены → транслитерация

**Примеры транслитерации:**
- `"хэллоу зыс из отец сентенцию инглиш"` (hello this is a test sentence in english)
- `"завязывать туда и сани набором"` (the weather today sunny and warm)

---

## 5️⃣ `SegmentMerger.java` - Объединение сегментов из двух моделей

**Назначение:** Объединяет сегменты от русской и английской моделей, выбирая лучший результат для каждого временного интервала.

**Основная задача:** Автоматически выбрать правильный сегмент для каждого временного интервала, чтобы в итоге получить один файл субтитров с корректным распознаванием обоих языков.

**Главный метод:** `mergeSegments(List<TranscriptionSegment> segmentsRu, List<TranscriptionSegment> segmentsEn) -> List<TranscriptionSegment>`

**Алгоритм работы:**
1. Создает временную шкалу из всех сегментов обеих моделей
   - Собирает все временные точки (начало и конец) из всех сегментов
   - Сортирует по возрастанию
2. Создает временные интервалы между соседними точками
3. Для каждого интервала находит лучший перекрывающийся сегмент из каждой модели
4. Выбирает лучший сегмент на основе языка, качества и транслитерации
5. Объединяет соседние сегменты с одинаковым текстом

**Метод `selectBestSegment()` - Логика выбора:**

Обрабатывает 4 основных случая:

**Случай 1: RU содержит кириллицу, EN содержит только латиницу**
- Идеальный случай: обе модели распознали свой язык
- Проверяет транслитерацию русского сегмента
- Если транслитерация → выбирает английский
- Иначе выбирает по качеству, с приоритетом русскому при похожем качестве

**Случай 2: RU содержит кириллицу, EN смешанный или не содержит латиницы**
- Русская модель правильно распознала русскую речь
- Проверяет транслитерацию
- Если транслитерация → выбирает английский (даже если он плохой)
- Иначе выбирает русский

**Случай 3: EN содержит только латиницу, RU не содержит кириллицы**
- Английская модель правильно распознала английскую речь
- Выбирает английский без дополнительных проверок

**Случай 4: Оба сегмента смешанные или неопределенные**
- Выбирает по качеству, но с учетом транслитерации
- Если русский - транслитерация → предпочитает английский

**Критерии выбора:**
1. **Соответствие алфавита модели** (кириллица для русской, латиница для английской)
2. **Отсутствие транслитерации** (русская модель не должна транслитерировать английскую речь)
3. **Качество распознавания** (больше слов, однородный алфавит)

**Метод `findBestOverlappingSegment()`:**
- Находит сегмент с наибольшим перекрытием (overlap) с временным интервалом
- Вычисляет пересечение интервалов: `min(segment.end, endTime) - max(segment.start, startTime)`

**Метод `mergeAdjacentSegments()`:**
- Объединяет соседние сегменты с одинаковым текстом
- Устраняет дубликаты, возникшие при разбиении на интервалы

---

## 6️⃣ `SubtitleGenerator.java` - Генерация SRT субтитров

**Назначение:** Преобразует список `TranscriptionSegment` в файл формата SRT.

**Главный метод:** `generateSRT(List<TranscriptionSegment> segments, Path outputPath)`

**Алгоритм работы:**
1. Открывает файл для записи (UTF-8)
2. Для каждого сегмента записывает:
   ```
   номер_сегмента
   время_начала --> время_конца
   текст
   пустая_строка
   ```
3. Форматирует время как `HH:MM:SS,mmm` (формат SRT)

**Метод `formatTime(double seconds) -> String`:**
- Переводит секунды в формат `00:00:05,500`
- Часы: `(int) (seconds / 3600)`
- Минуты: `(int) ((seconds % 3600) / 60)`
- Секунды: `(int) (seconds % 60)`
- Миллисекунды: `(int) ((seconds % 1) * 1000)`

**Пример вывода:**
```
1
00:00:00,780 --> 00:00:04,019
hello this is a test

2
00:00:04,019 --> 00:00:08,130
it contains different words
```

---

## 7️⃣ `SpeechRecognitionApp.java` - Главный класс

**Назначение:** Координирует работу всех сервисов и управляет потоком выполнения.

**Константы:**
- `DEFAULT_AUDIO_RESOURCE` - дефолтный файл для запуска из IDE
- `MODEL_RU` - путь к русской модели (`models/vosk-model-small-ru-0.22`)
- `MODEL_EN` - путь к английской модели (`models/vosk-model-small-en-us-0.15`)

### Метод `main(String[] args)` - Точка входа

**Алгоритм:**

**1. Парсинг аргументов:**
```java
if (args.length < 1) {
    args = getDefaultArgs(); // Загружает /video_en.mp4 из ресурсов
}
String inputFilePath = args[0]; // Путь к файлу
```

**2. Обработка ресурсов:**
```java
if (inputFilePath.startsWith("/")) {
    inputFile = extractResourceToTemp(inputFilePath);
    // Копирует файл из JAR в C:\Temp\resource_audio_12345.mp4
}
```

**3. Инициализация сервисов:**
```java
AudioExtractor audioExtractor = new AudioExtractor();
SubtitleGenerator subtitleGenerator = new SubtitleGenerator();
```

**4. Извлечение аудио:**
```java
Path audioFile = extractAudio(audioExtractor, inputFile);
// Результат: C:\Temp\audio_67890.wav (16kHz mono)
```

**5. Распознавание ДВУМЯ моделями:**
```java
// Русская модель
List<TranscriptionSegment> segmentsRu = recognizeSpeech(MODEL_RU, audioFile);

// Английская модель  
List<TranscriptionSegment> segmentsEn = recognizeSpeech(MODEL_EN, audioFile);
```

**6. Объединение сегментов и генерация субтитров:**
```java
SegmentMerger segmentMerger = new SegmentMerger();
generateSubtitles(segmentsRu, segmentsEn, inputFilePath, subtitleGenerator, segmentMerger);
// Создает output/video.srt (один объединенный файл)
```

**7. Очистка:**
```java
finally {
    cleanupTempFile(audioFile); // Удаляет временный WAV
}
```

### Вспомогательные методы:

**`extractAudio(AudioExtractor extractor, Path inputFile) -> Path`**
- Обертка над `audioExtractor.extractAudio()`
- Логирует процесс извлечения

**`recognizeSpeech(String modelPath, Path audioFile) -> List<TranscriptionSegment>`**
- Проверяет наличие модели
- Создает `SpeechRecognitionService`
- Вызывает распознавание
- Закрывает сервис через `finally`

**`generateSubtitles(segmentsRu, segmentsEn, inputFilePath, generator, merger)`**
- Объединяет сегменты из двух моделей через `SegmentMerger`
- Автоматически выбирает лучший сегмент для каждого временного интервала
- Определяет базовое имя файла (без расширения)
- Создает папку `output/` если её нет
- Генерирует один файл: `basename.srt` (объединенный результат)

**`extractResourceToTemp(String resourcePath) -> Path`**
- Извлекает файл из JAR в временную папку
- Используется для запуска из IDE без аргументов
- Файл помечается на удаление при выходе

**`cleanupTempFile(Path tempFile)`**
- Удаляет временный WAV файл
- Обрабатывает ошибки (не критичные)

**`printUsage()`**
- Выводит справку по использованию программы
- Показывает пути к моделям и примеры запуска
- Указывает, что создается один объединенный файл субтитров

---

## 🔄 Полный поток выполнения

**Входные данные:** `video.mp4` (английская речь)

```
1. SpeechRecognitionApp.main()
   └─> Принимает "video.mp4"

2. AudioExtractor.extractAudio()
   └─> video.mp4 → C:\Temp\audio_123.wav (16kHz mono)

3. SpeechRecognitionService (RU model)
   ├─> Загружает models/vosk-model-small-ru-0.22/
   ├─> Читает audio_123.wav по 4096 байт
   ├─> Vosk пытается распознать как русский
   └─> Результат: [*мусор*]

4. SpeechRecognitionService (EN model)
   ├─> Загружает models/vosk-model-small-en-us-0.15/
   ├─> Читает audio_123.wav по 4000 байт
   ├─> Накапливает все слова
   ├─> Создает сегменты на основе пауз (> 0.5 сек)
   └─> Результат: ["hello this is a test", "today is november"] (правильно)

5. SegmentMerger
   ├─> Создает временную шкалу из всех сегментов
   ├─> Для каждого интервала выбирает лучший сегмент
   ├─> Проверяет язык, качество и транслитерацию
   └─> Результат: объединенный список с правильными сегментами

6. SubtitleGenerator
   └─> Генерирует output/video.srt (один файл с автоматическим выбором языка)

7. Cleanup
   └─> Удаляет C:\Temp\audio_123.wav
```

**Результат:** Один файл субтитров с автоматическим выбором правильной модели для каждого сегмента.

---

## 🛠️ Зависимости и их роль

**1. Vosk (`com.alphacephei:vosk:0.3.45`)**
- Движок распознавания речи (Kaldi-based)
- Работает оффлайн
- Поддерживает 20+ языков
- Выдает результаты с таймкодами

**2. JNA (`net.java.dev.jna:jna:5.13.0`)**
- Java Native Access
- Позволяет вызывать нативные библиотеки Vosk из Java
- Автоматически подгружает `.so`/`.dll` файлы

**3. Jave2 (`ws.schild:jave-all-deps:3.5.0`)**
- Java Audio Video Encoder
- Содержит встроенный FFmpeg
- Конвертирует любые форматы в WAV 16kHz mono

**4. Gson (`com.google.code.gson:2.10.1`)**
- Парсинг JSON ответов от Vosk
- Десериализация `{"result": [...], "text": "..."}`

**5. SLF4J (`org.slf4j:slf4j-simple:2.0.9`)**
- Simple Logging Facade
- Выводит INFO/ERROR логи в консоль

---
